type IconType = React.ElementType;

interface FeatureCardProps {
  icon: IconType;
  title: string;
  description: string;
}

interface StepCardProps {
  number: string;
  title: string;
  description: string;
}